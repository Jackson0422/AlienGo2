================================================================
Domain Randomization Summary
Env: AlienGoFlatEnvCfg  (cat_flat_env_cfg.py)
================================================================

1. Rigid-body physics material (mode = startup)
   - Bodies        : robot/.* (all links)
   - Static friction  : Uniform(0.3, 1.5)
   - Dynamic friction : Uniform(0.3, 1.5)
   - Restitution      : Uniform(0.0, 0.1)
   - num_buckets      : 100

2. Trunk mass (mode = startup)
   - Body            : trunk
   - Scale           : Uniform(0.75, 1.05)   (~ -25% / +5%)
   - recompute_inertia : True

3. Leg link mass (mode = startup)
   - Bodies          : *_hip, *_thigh, *_calf
   - Scale           : Uniform(0.90, 1.30)
   - recompute_inertia : True

4. Rotor mass (mode = startup)
   - Bodies          : *_rotor
   - Scale           : Uniform(0.00, 1.10)
   - recompute_inertia : True

5. PD actuator gains (mode = reset, every episode)
   - Joints          : all
   - Stiffness scale : Uniform(0.95, 1.05)   (+/-5%)
   - Damping   scale : Uniform(0.95, 1.05)   (+/-5%)

6. Base initial state (mode = reset)
   - Pose:    x  in (-0.05, 0.05) m
              y  in (-0.05, 0.05) m
              yaw fixed at 0
   - Vel :    roll  in (-0.3, 0.3) rad/s
              pitch in (-0.3, 0.3) rad/s
              lin/yaw = 0

7. Joint initial state (mode = reset)
   - Position scale : Uniform(0.9, 1.1)
   - Velocity       : Uniform(-0.1, 0.1) rad/s

8. Observation noise (additive uniform, enable_corruption = True)
   - base_ang_vel     : +/-0.001
   - base_lin_vel     : +/-0.1
   - base_height      : +/-0.01
   - projected_gravity: +/-0.05
   - joint_pos        : +/-0.01
   - joint_vel        : +/-0.2
   - rear_fz          : +/-5.0  N
   - com_cop_xy       : +/-0.01 m
   (disabled in PLAY config)

Notes:
- All randomizations above are disabled in AlienGoFlatEnvCfg_PLAY
  except physics_material and the reset-state randomizations,
  to keep evaluation reproducible.
- No external force/torque push is currently applied
  (push_trunk_force is commented out).
================================================================
